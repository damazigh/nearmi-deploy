stages:
  - deploy
env:
  stage: .pre
  script:
    - >
        echo ${TARGET_ENV}
        .
  tag: shell
auth:
  stage: deploy
  script:
    - echo "Starting authentication services..."
    - ls -lart
    - docker-compose -f ./keycloak/auth-dev.yml up
  tag: shell
    
  